---
interface Props {
	number: string;
	label: string;
	text: string;
}

const { number, label, text } = Astro.props;
---

<article class="lately-item">
	<span class="lately-number">{number}</span>
	<h3>{label}</h3>
	<p>{text}</p>
</article>
